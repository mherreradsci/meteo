# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## Commands

```bash
# Run the app
streamlit run app.py

# Install dependencies (uses venv)
source venv/bin/activate
pip install -r requirements.txt

# Download the Chilean communes GeoJSON (required before first run)
python scripts/descargar_comunas.py
```

Python version: 3.12.4 (see `.python-version`).

## Architecture

This is a single-page Streamlit app that lets users find Chilean territories within a target temperature range — intended for pisciculture + aquaponics site selection.

**Data flow:**
1. User defines area (commune by name/CUT code, or lat/lon polygon) and date range
2. `geo.py` resolves the Shapely geometry and samples a regular lat/lon grid inside it
3. `clima.py` queries the Open-Meteo Archive API (ERA5, free) for daily Tmin/Tmax/Tmean for each grid point
4. `analisis.py` aggregates per-point stats and marks points as *apto* (suitable) based on the temperature range
5. `mapa.py` renders a Folium map; `graficos.py` renders Plotly charts

**Package layout:**

```
app.py                  # Streamlit entry point — UI, session_state, orchestration
src/meteo/
  geo.py                # GeoJSON loading, commune lookup (name/CUT), grid sampling
  clima.py              # Open-Meteo HTTP client, request batching (50 pts/batch), disk cache
  analisis.py           # Per-point aggregation and aptitude filter
  mapa.py               # Folium map with CircleMarkers coloured by Tmean
  graficos.py           # Plotly time-series and scatter distribution charts
data/
  comunas_chile.geojson # Shapefile of all Chilean communes (generated by descargar_comunas.py)
  cache/openmeteo_cache.sqlite  # requests_cache SQLite store (7-day TTL)
```

**Key data contracts:**

- `geo.muestrear_grilla()` → `list[(lat, lon)]`
- `clima.obtener_temperatura_diaria()` → `list[pd.DataFrame]` (one per point, columns: `tmin`, `tmax`, `tmean` — where `tmean` is the ERA5 24-hour hourly mean for that day; DatetimeTZ index in `America/Santiago`)
- `analisis.agregar_puntos()` → `pd.DataFrame` with columns: `lat`, `lon`, `tmin_periodo`, `tmax_periodo`, `tmean_periodo`, `datos_ok`
- `analisis.filtrar_aptos()` → same DataFrame with added `apto` (bool) column

**API notes:**

- Open-Meteo Archive API (`archive-api.open-meteo.com/v1/archive`) is free and requires no key.
- The API accepts bulk requests: multiple lat/lon lists in a single call. Returns a list of dicts when multiple points are sent, a single dict when only one point is sent — `clima.py` normalises this difference.
- Responses are cached in SQLite via `requests_cache` (7-day TTL) to avoid redundant API calls during development.

**Aptitude criterion:**
A point is *apto* if `tmin_periodo >= temp_min AND tmax_periodo <= temp_max` (i.e., no day ever left the desired range). Points with missing data (`datos_ok == False`) are always non-apto.
